<template>
  <div class="border-2 border-red-600 text-center text-8xl">
    00:00
  </div>
</template>

<script setup>

const props = defineProps(["type"]);
const emit = defineEmits(["chooseTheme"]);

</script>
